services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: workflow-studio-sqlserver
    environment:
      ACCEPT_EULA: Y
      MSSQL_SA_PASSWORD: Your_strong_password123!
    ports:
      - "1433:1433"
    volumes:
      - workflowstudio-sql:/var/opt/mssql
volumes:
  workflowstudio-sql:
