import {Row} from '@/types/workflow';
export function validateRows(rows:Row[]){const errors:{row:string,field:string,message:string}[]=[];rows.forEach((r,i)=>{if(!r.from)errors.push({row:String(i+1),field:'from',message:'From is required'});if(!r.to)errors.push({row:String(i+1),field:'to',message:'To is required'});if(!r.label)errors.push({row:String(i+1),field:'label',message:'Trigger / label is required'})});const refs=new Set(rows.flatMap(r=>[r.from,r.to].filter(Boolean)));rows.forEach((r,i)=>{if(r.to&&!refs.has(r.to))errors.push({row:String(i+1),field:'to',message:`Unknown reference: ${r.to}`})});return errors}
